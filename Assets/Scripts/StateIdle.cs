using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/// <summary>
/// if the player is not moving, attacking, or dodging they should be idle, and able to move their camera around.
/// </summary>
public class StateIdle : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
